const initialState = [{
    id: "box1",
    cx: 600,
    cy: 200,
    r: 10,
    stroke: "black",
    strokeWidth: 3,
    fill: "black"
}];

const setting = (state = initialState, action) => {
    return state;
};

export default setting;