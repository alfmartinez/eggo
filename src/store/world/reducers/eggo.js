const initialState = {
    cx: 400,
    cy: 200,
    r: 10,
    stroke: "black",
    strokeWidth: 3,
    fill: "white"
};

const eggo = (state = initialState, action) => {
    return state;
};

export default eggo;