const initialState = [{
    id: "mob1",
    cx: 200,
    cy: 200,
    r: 10,
    stroke: "red",
    strokeWidth: 3,
    fill: "red"
}];

const mobs = (state = initialState, action) => {
    return state;
};

export default mobs;